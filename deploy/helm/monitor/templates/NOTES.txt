monitor 已部署({{ include "monitor.fullname" . }})。

镜像: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
conf.d ConfigMap:{{ .Values.configMapName }}(本 chart 建 00-base.conf 阈值,挂到 /etc/monitor/conf.d)
dashboard: Service :{{ .Values.service.port }}

配置更新两种方式(不冲突;monitor 60s 自热加载,无需 sidecar):
  - autoconfig(可选):有 ModelRoute(monitor.outputConfigMap={{ .Values.configMapName }})时自动写/更新 <model>.monitor.conf;
  - 手动:kubectl edit cm {{ .Values.configMapName }} 增删 service 行。
  Helm upgrade 三方合并不会覆盖 autoconfig/手动加的 service key。
