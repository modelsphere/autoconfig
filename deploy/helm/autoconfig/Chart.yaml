apiVersion: v2
name: autoconfig
description: k8s 后端发现 → 自动同步 openresty peers / cache-aware-router workers(ModelRoute controller)
type: application
version: 0.3.41        # chart 版本
appVersion: "0.3.41"   # 镜像版本(image.tag 缺省用它)
keywords: [routing, openresty, cache-aware-router, service-discovery, operator]
home: https://gitlab.4pd.io/inference-production-stack/autoconfig
