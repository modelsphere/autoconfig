apiVersion: v1
kind: Service
metadata:
  name: {{ include "cart.fullname" . }}
  labels: {{- include "cart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "cart.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
